task_id: "invalid-max-iterations"
source_issue: 9003
objective: "Should fail because max_iterations is outside 0-5."
risk_level: "green"
merge_policy: "auto_merge_if_green"
max_iterations: 99
allow_auto_fix: true
allowed_file_patterns:
  - "README.md"
risk_reasoning:
  - "max_iterations exceeds the allowed bound."
implementation_units:
  - id: "unit-1"
    goal: "Should not validate."
    verification:
      - "true"
acceptance_criteria:
  - "Validation fails."
verification_commands:
  - "true"
review_requirements:
  - "Validation must fail."
